// adalah sebuah tipe data simple yang hanya memiliki 2 value yaitu antara true dan falsee

const bool = true;
console.log(typeof bool, bool);

const newBool = Boolean(10);
console.log(typeof newBool, newBool);
